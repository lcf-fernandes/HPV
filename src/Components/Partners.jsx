import React from "react";
import * as RN from "react-native-web";

export const Partners = () => {
  return (
    <RN.View style={stlMain}>
      <RN.Text>Partners</RN.Text>
    </RN.View>
  );
};

// ---------- set Styles
const stlMain = {
  flex: 1
};
