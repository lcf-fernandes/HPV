import React from "react";
import * as RN from "react-native-web";

export const Contact = () => {
  return (
    <RN.View style={stlMain}>
      <RN.Text>Contact</RN.Text>
    </RN.View>
  );
};

// ---------- set Styles
const stlMain = {
  flex: 1
};
